[
  {
    "name": "/status",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Check payment/order status by ID (shows latest order if no ID provided)"
            }
          }
        ],
        "send": {
          "text": "/status"
        }
      }
    ]
  }
]
